-- Insert a site
-- Execute using npx wrangler d1 execute index_db --file scripts/insert_sites.sql

-- INSERT INTO sites (name, sitemap_url)
  -- VALUES ('Example Site', 'http://example.com/sitemap.xml');